<?php
class Sendmail {
	function toAdmin($text = null) {
	
	/**
	 * send email to ADMINISTRATOR_EMAIL
	 * with
	 * __FILE__
	 * __LINE__
	 *
	 * $_SERVER
	 * $_POST
	 * $_GET
	 * $_SESSION
	 * $_COOKIE
	 */
	}
}
